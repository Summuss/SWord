<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />


    <!-- loading主题 -->
    <style name="LunchLoadingTheme" parent="Theme.AppCompat">
        <!--        <item name="windowActionBar">false</item>-->

        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <!--下面这句如果你加上觉得好看就加，不好看就不加咯-->
        <!--<item name="android:navigationBarColor">@color/transparent</item>-->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@drawable/launch_loading_good_day</item>
    </style>

    <!-- floatButtonMenu 的主题 -->
    <style name="menu_labels_style">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <!-- 输入框错误提示 -->
    <style name="HintError" parent="TextAppearance.AppCompat">
        <item name="android:textColor">#F30657</item>
    </style>

    <style name="Custom.Widget.EditView" parent="Widget.AppCompat.EditText">
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="colorControlNormal">@color/colorPrimary</item>
        <!-- AppCompatEditText选择的底线颜色 -->
        <item name="colorControlActivated">@color/colorPrimary</item>
    </style>

    <style name="TreeNodeStyle">
        <item name="android:paddingLeft">20dp</item>
    </style>

</resources>
